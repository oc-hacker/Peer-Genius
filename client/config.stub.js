// URL of the server to access. Defaults to localhost (for development purposes)
export const serverURL = 'http://localhost:8080';

// Fill out only if providing a livestream token.
export const STREAM_LINK = '';

export default serverURL;
